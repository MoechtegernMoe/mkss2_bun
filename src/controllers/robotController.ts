import { items } from '../data/itemsData';

import type { Request, Response } from 'express';

import { robots } from '../data/robotsData';

export async function attackRobot(req: Request, res: Response) {}
